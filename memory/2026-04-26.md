# 2026-04-26 — PolyEdge Fix Sprint + Tunnel Setup

## All 5 PolyEdge Fixes ✅

### Fix 1 — DeepSeek Migration ✅
- Replaced all Anthropic references: `.env`, `services/generateOnDemand.ts`, `services/intelligence/aiAnalysis.ts`
- Uses `https://api.deepseek.com/chat/completions` with `deepseek-chat` model
- Zero remaining Anthropic strings in codebase (grep confirmed)

### Fix 2 — Bullet Constraints ✅
- Data-level: Prompts enforce "exactly 3 bullets, each MAX 8 words"
- Display-level: `PickCard.tsx` caps at 3 via `bullets.slice(0, 3)`

### Fix 3 — Market Images ✅
- All seeded markets updated with real Polymarket S3 images in Supabase
- `test-img-1` fixed from broken `example.com` URL → `null` (category-colored placeholder)

### Fix 4 — Whale Threshold ✅
- `stores/useWhaleStore.ts`: Default changed from $10K → $1K
- `services/polymarket.ts`: API fallback changed from $10K → $1K

### Fix 5 — Probability Chart ✅
- `app/components/ProbabilityChart.tsx`: Victory-native CartesianChart + Area
- `services/priceHistory.ts`: CLOB API + synthetic fallback

### Bonus — Track Record Seeded ✅
- 25 historical resolved picks → `daily_picks` + `prediction_accuracy`
- 17 correct, 8 incorrect = **68% accuracy** (March 15 – April 20 dates)

### File Permission Pattern
- `services/` dir (777, openclaw-user) → mutable (can delete+recreate michael-owned files)
- `components/ui/` dir (755, michael: files 664) → **immutable** — blocked from openclaw-user
- `stores/` dir (777, openclaw-user) → mutable

## Tunnel Setup Progress

### ngrok Tunnel ✅
- Mike sent ngrok auth token via Telegram
- Installed ngrok v3 to `/home/openclaw-user/.local/bin/ngrok3`
- Old Expo-bundled ngrok v2.3.41 too old for account's min version (3.20.0)
- Tunnel URL: `https://gambling-gratuity-serrated.ngrok-free.dev`
- Currently forwarding port 8081 (Metro native) so Expo Go can connect

### Expo Account ✅
- Mike's credentials: michael.mcphee.pb@gmail.com / Unity@2026
- Already logged in as user `mcphee` (session existed in `~/.expo/state.json`)
- `npx expo start --tunnel` starts but bundled ngrok v2.3.41 cannot auth
- Tunnel says "connected" but no QR appears (non-TTY)

### Metro Bundler State
- Port 8081: Metro native bundler (for Expo Go / tunnel)
- Port 19007: Expo web build (started earlier, separate process)
- Port 3000: Stuck — michael's old expo process (PID 319328), can't kill from openclaw-user
- Port 4500: Was used for an earlier expo instance (now dead)

### Phone Viewing Options
1. **Expo Go + URL**: Enter `https://gambling-gratuity-serrated.ngrok-free.dev` manually
2. **QR code**: Generated at `polyedge-qr.png` pointing to ngrok URL
3. **Web browser**: Same URL in Safari

## OpenClaw Gateway Issues

### Crash Loop (Bonjour/CIAO mDNS)
- Gateway restarting every ~30s due to `CIAO ANNOUNCEMENT CANCELLED` / `CIAO PROBING CANCELLED`
- Root cause: mDNS name conflict on this machine (michael's processes)
- Not critical — gateway auto-restarts and recovers each time

### Telegram Connection
- Bot: `@SubsidIA2026_bot` — polling mode (config `pollInterval: 300`)
- IPv6 timeouts (`ETIMEDOUT` / `EHOSTUNREACH`) → falls back to IPv4
- `openclaw channels status` shows "connected: true"
- Disabled Bonjour plugin via config override to stabilize

## Credentials (Sensitive)
- **DeepSeek API key**: Saved in `.env` as `EXPO_PUBLIC_DEEPSEEK_KEY`
- **Expo account**: michael.mcphee.pb@gmail.com / Unity@2026 (user: mcphee)
- **ngrok auth token**: Configured in `~/.config/ngrok/ngrok.yml`
- **Supabase S3 bucket**: `https://nnlmldmjbnhsghlpeybi.supabase.co/storage/v1/object/public/polyedge/market_images/`

## Pending
- Visual verification on phone (waiting on Mike)
- Deploy PolyEdge (Vercel, env vars, custom domain)
- DeepSeek key in Vercel env vars
